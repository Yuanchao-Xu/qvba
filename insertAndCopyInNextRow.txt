Sub insertAndCopyInNextRow()
    Dim startRow As Integer
    Dim startColumn As Integer
    
    ' When it goes to the 2nd row, it does insertion, and copy the 1nd row
    ' to the inserted row, and go on.
    
    startColumn = 2
    startRow = 3
    
    While Cells(startColumn, startColumn) <> ""
        
    End
    
    
    
    
    For i = 3 To 46
        Range(Cells(row1, column), Cells(row2, column)).Select
        With Selection
            .HorizontalAlignment = xlCenter
            .VerticalAlignment = xlCenter
            .WrapText = False
            .Orientation = 0
            .AddIndent = False
            .IndentLevel = 0
            .ShrinkToFit = False
            .ReadingOrder = xlContext
            .MergeCells = False
    End With
    Selection.Merge
    Next
    
End Sub